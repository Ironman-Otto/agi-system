You are reviewing the latest AGI/CAS repository code bundle.

Uploaded files:
1. A full text code bundle generated by repo_bundle.ps1.
2. A manifest JSON file generated by repo_bundle.ps1.

Analysis instructions:
- Treat the uploaded bundle as a read-only snapshot of the current repository.
- Do not assume this bundle updates or replaces work in any other conversation.
- First read the manifest to understand file coverage, bundle type, version, Git branch, commit hash, and included files.
- Then inspect the full text bundle using the file markers:
  # FILE: path/to/file
- Build a high-level map of the current codebase.
- Identify major modules, especially CMB, AEM, NLP, planner, message schemas, message handlers, logging, intent extraction, and test/demo files.
- Check whether the files needed for the Directive Processing Loop are present.
- Identify missing files, suspicious omissions, duplicated files, stale files, or files that appear inconsistent with the CAS architecture.
- Do not generate replacement code unless explicitly asked.
- Do not recommend changing working code in the Directive Processing Loop conversation unless the issue is clearly confirmed from the uploaded bundle.
- Provide a structured review with:
  1. Bundle validation
  2. Repository/module map
  3. Key architecture observations
  4. Possible risks or inconsistencies
  5. Recommended next review steps
  6. Questions to resolve before making code changes

Important:
This is an analysis-only review. Any proposed changes should be listed separately and should not be treated as automatically approved.